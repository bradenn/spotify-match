package api

/*import (
	b64 "encoding/base64"
	"fmt"
	"log"
	"net/http"
	"os"
)

func AuthenticateCredentials() {
	client := os.Getenv("SPOTIFY_CLIENT")
	secret := os.Getenv("SPOTIFY_SECRET")

	/*code := b64.RawStdEncoding.EncodeToString([]byte(fmt.Sprintf("%s:%s", client, secret)))

	response, err := http.Get("https://accounts.spotify.com/api/token")
	if err != nil {
		log.Fatalln(err)
	}*/
	/*client, err := http.ListenAndServe()
	response.
*/

/*
}*/
